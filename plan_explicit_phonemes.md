

Plan for extending seedvox from implicit phonemes to using an explicity phoneme planner and generator that conditions the acoustic token generation as follows:
1. The Char and BPE tokens are used jointly as input to an explicit phoneme token planner planner. This can be an AR transformer model as the input sequence of the aligned joint Char and BPE tokens can be of different lenght that the phoneme tokens.  We train this module by leveraging the data generated by external g2p module (a generator) such as deep-phonemizer.  The phonemes must be contextualize, meaning computed on the entire input sequence, not word-by-word.  The generate phonemes must be matching the external g2p planner, do not include padding tokens etc.  You can and must insert punctuation tokens in the Phonetic sequence. 
2. The joint Char and BPE tokens are used as input to an explicit JEPA prosody planner. 
3. The joint Char and BPE tokens are next augmented with the output of the JEPA planner (prosody tokens) and the output of the phonemetic planner (phonetic tokens) to create the structured input to train and run the AR acoustic decoder.
4. The whole systems is a chain-of-thought system.  
    - From text (chars and BPEs) - > Phoneme tokens
    - From text (chars and BPE) -> Prosody latents (JEPA block)
    - From text (chars and BPE) + Phoneme tokens + Prosody Latents -> Acoustic mimi tokens
    - From Acoustic Mimi tokens -> Speech.

Here are some rules:
1. Learn how the acoustic decoder is used in the current seedvox module. The parameter, the structure must remain the same.
2. The explicit phonemes must be able to be overwritten by hand to correct for spelling problems in the internalized g2p. This will enable spelling variants and a tool for correcting TTS spelling.
3. Create new files and a new subdir  call 'explicit_pros_phon_planner' in the current directory. All files for this task are located there. 
4. Do not overwrite files, ask for explicit verification.
5. Create test scripts to:
    - verifiy the correctness of the g2p prediction as output of the phonetic planner.
    - run training. Use the same options and command lines as the current seedvox trainer.
    - run inference. Use the same options and command lines as the current seedvox infer script to add on top an input option that will overwrite the planned phonemes.

Your role:
1. You are an expert in speech generation and an expert in efficient pytorch programming.
2. You are a Phd student, eager to think critical and ask questions and show value to improve over SOTA.
3. I am your professor, ask question to me to cross check and to validate your thinking and code generation. If anything is unclear ask questions rather than assuming..
4. Use a subagent for efficient implementation and separate context tasks.


